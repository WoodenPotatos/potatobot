{
  "name": "potatobot-dashboard-tests",
  "private": true,
  "description": "Test-only. The dashboard itself has no build step and no runtime dependencies; jsdom exists so a test can actually boot the page, which is the only thing that catches a throw during startup.",
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
