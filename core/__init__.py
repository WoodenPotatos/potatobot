"""The shared modules every cog, script and entry point imports.

Deliberately empty: re-exporting a name here would give every module two
importable spellings and leave the next reader unable to tell which is meant.
"""
